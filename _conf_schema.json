{
  "enabled": {
    "description": "Enable raw LogisticsAI message uploads.",
    "type": "bool",
    "default": true
  },
  "api_url": {
    "description": "Full URL of the LogisticsAI raw message endpoint.",
    "type": "string",
    "hint": "Example: https://eshinetong.com/api/messages",
    "default": "http://127.0.0.1:5000/api/messages"
  },
  "api_token": {
    "description": "Backend API token. Leave empty when authentication is not required.",
    "type": "string",
    "hint": "For Authorization, enter either a raw token or Bearer followed by the token.",
    "default": ""
  },
  "token_header": {
    "description": "HTTP request header used for the API token.",
    "type": "string",
    "hint": "Common values: Authorization or X-API-Key",
    "default": "Authorization"
  },
  "timeout": {
    "description": "Timeout in seconds for the raw-message upload request.",
    "type": "float",
    "default": 30.0
  },
  "assistant_reply_timeout": {
    "description": "Timeout in seconds for forwarding an intercepted native reply to the backend update endpoint.",
    "type": "float",
    "default": 300.0
  },
  "retry_count": {
    "description": "Number of retries after a failed backend request, from 0 to 10.",
    "type": "int",
    "default": 3
  },
  "retry_interval": {
    "description": "Initial retry delay in seconds. Later retries use exponential backoff.",
    "type": "float",
    "default": 1.0
  },
  "verify_ssl": {
    "description": "Verify the backend TLS certificate for HTTPS requests.",
    "type": "bool",
    "default": true
  },
  "max_concurrency": {
    "description": "Maximum number of concurrent backend HTTP requests.",
    "type": "int",
    "default": 4
  }
}
